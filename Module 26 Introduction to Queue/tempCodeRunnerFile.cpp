    if (!q.empty())
    {
        myType chk;
        chk = q.Front();
        cout << chk.first << " | " << chk.second << endl;
    }
    if (!q.empty())
    {
        myType chk;
        chk = q.Back();
        cout << chk.first << " | " << chk.second << endl;
    }